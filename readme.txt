The yaffs integration performed here is using the direct module. To upgrade, follow the
README instructions in src/direct and copy the updated yaffs_xxxx files into the yaffs2/direct
directory. This will remove the Linux dependencies in the project.